package com.coderhouse.services;



/*import java.util.List;*/

/*import org.springframework.beans.factory.annotation.Autowired;*/
import org.springframework.stereotype.Service;

/*import com.coderhouse.models.Detalle;
import com.coderhouse.repositories.DetalleRepository;

import jakarta.transaction.Transactional;*/


@Service
public class DetalleService {

	/*
	 * @Autowired private DetalleRepository PedidoRepository;
	 * 
	 * //*********GetAllPedidos
	 ***********/

	/*public List<Detalle> getAllPedidos() {
			return PedidoRepository.findAll();
		}*/


 //*********GetPedidoByID************
	/*
	 * public Detalle findById(Long id) { return
	 * PedidoRepository.findById(id).orElseThrow(() -> new
	 * IllegalArgumentException("Pedido no encontrado")); }
	 */



	//**********AddNewPedido**********//
	/*
	 * @Transactional public Detalle crearPedido(Detalle Pedido) { return
	 * PedidoRepository.save(Pedido); }
	 * 
	 */




	//*********DeletePedidoByID**********//
	/*
	 * public void deletePedido(Long id) { if (!PedidoRepository.existsById(id)) {
	 * throw new IllegalArgumentException("Pedido no encontrado"); }
	 * PedidoRepository.deleteById(id); }
	 * 
	 */

	}
